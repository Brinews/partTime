#include <iostream>

using namespace std;

int main() {
    int *a;     // we use a pointer to an integer

    // implement code asking the user to enter a, and store it in *a - be careful



    int *numbers;   // pointer to an array(!) to store integers
    numbers=new int[*a];      // here we ask for a `slots' containing integers

    // implement code asking the user `a' times to enter the integers, and store them in the array numbers


    cout << "The numbers divisible by 5 are: " << endl;

    //implement code outputting all numbers entered by the user which are divisible by 5

}
